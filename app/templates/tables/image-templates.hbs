<div class="widget list-table">
  <div class="table-head-ribbon">
    <div>
      Total: {{total}} templates
    </div>
    <div class="ui mini icon input">
      {{input type='text' value=esFilter placeholder='Search...'}}
      <i class="search icon"></i>
    </div>
  </div>

  <div class="table-box">
  <table>
    <thead>
      <tr>
        <th {{action 'sortBy' 'id'}}>ID</th>
        <th {{action 'sortBy' 'name'}}>Name</th>
        <th {{action 'sortBy' 'publicFlag'}}>Public?</th>
        <th {{action 'sortBy' 'flexImageFlag'}}>FlexImage?</th>
        <th {{action 'sortBy' 'globalIdentifier'}}>globalID</th>
        <th {{action 'sortBy' 'virtualServers.length'}}>VMs</th>
      </tr>
    </thead>
    <tbody>
      {{#each filteredContent as |item|}}
      <tr {{action "select" item}} class="clickable {{item.mark}}">
        <td class="tac">{{item.id}}</td>
        <td class="tac">{{item.name}}</td>
        <td class="tac">{{item.publicFlag}}</td>
        <td class="tac">{{item.flexImageFlag}}</td>
        <td class="tac">{{item.globalIdentifier}}</td>
        <td class="tac">{{item.virtualServers.length}}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>
  </div>
</div>
