<div class="widget list-table">
  <div class="table-head-ribbon">
    <div class="table-title">Servers</div>
    <div>
      Total: {{total}} servers
    </div>
    <div class="ui mini icon input">
      {{input type='text' value=esFilter placeholder='Search...'}}
      <i class="search icon"></i>
    </div>
  </div>

  <div class="table-box">
  <table class="styled filled">
    <thead>
      <tr>
        <th {{action 'sortBy' 'id'}}>ID</th>
        <th {{action 'sortBy' 'hostname'}}>Hostname</th>
        <th {{action 'sortBy' 'domain'}}>Domain</th>
        <th {{action 'sortBy' 'cpus'}}>CPU</th>
        <th {{action 'sortBy' 'memories'}}>Memory</th>
        <th {{action 'sortBy' 'provisionDate'}}>Provision Date</th>
        <th {{action 'sortBy' 'primaryIpAddress'}}>Primary IP</th>
        <th {{action 'sortBy' 'os'}}>OS</th>
        <th {{action 'sortBy' 'hourlyBillingFlag'}}>Hourly?</th>
        <th {{action 'sortBy' 'billingFee'}}>Fee</th>
        <th {{action 'sortBy' 'status'}}>Status</th>
        <th {{action 'sortBy' 'powerState'}}>Power</th>
        <th {{action 'sortBy' 'datacenter.longName'}}>Center</th>
        <th {{action 'sortBy' 'imageTemplate.name'}}>Image</th>
      </tr>
    </thead>
    <tbody>
      {{#each filteredContent as |item|}}
      <tr {{action "select" item}} class="clickable {{item.flag}}">
        <td class="tac">{{item.id}}</td>
        <td class="tac">{{#ui-popup variation="small"
          html=item.popup}}{{item.hostname}}{{/ui-popup}}</td>
        <td class="tac">{{item.domain}}</td>
        <td class="tac">{{item.cpus}}</td>
        <td class="tac">{{item.memories}}</td>
        <td class="tac">{{moment-format item.provisionDate 'YYYY-MM-DD'}}</td>
        <td class="tac">{{item.primaryIpAddress}}</td>
        <td class="tac">{{item.os}}</td>
        <td class="tac">{{item.hourlyBillingFlag}}</td>
        <td class="tac">{{item.billingFee}}</td>
        <td class="tac">{{item.status}}</td>
        <td class="tac">{{item.powerState}}</td>
        <td class="tac">{{item.datacenter.longName}}</td>
        <td class="tac">{{item.imageTemplate.name}}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>
  </div>

  {{#ui-accordion class="help widget"}}
  <div class="title tar"><i class="grey help icon"></i></div>
  <div class="content widget help">
    <h5>서버 목록</h5>
    <p>
    사용자의 모든 서버가 표시됩니다. 원하는 서버를 클릭하면 해당 서버의
    상세정보가 함께 표시됩니다.
    </p>
    <dl>
      <dt></dt>
      <dd></dd>
    </dl>
  </div>
  {{/ui-accordion}}
</div>
